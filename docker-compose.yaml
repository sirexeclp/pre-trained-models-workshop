version: "3.9"
services:
  app:
    build: .
    image: whisper-demo
    # TODO: expose Port 8000 of the app container to the host
    environment:
      - TRANSFORMERS_CACHE=/models
    volumes:
      - ./models:/models
